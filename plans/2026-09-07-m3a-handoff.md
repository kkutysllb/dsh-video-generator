# dsh-video-generator M3a 通道规范化 + 提示词/Schema + 交接工具实施计划

**Goal:** 清偿 M2 审查的三项 MUST-FIX（拓扑规范化/estimate 注入链/轮询韧性），交付提示词四层模块、story/script/storyboard 三段 Schema 校验器，以及注册进 DSH 的三个交接工具（vgen_story/vgen_script/vgen_storyboard）——会话模型从此可以真正驱动"写故事→拆剧本→出分镜"并落盘开 run。

**Architecture:** 拓扑规范化采用「通道 baseUrl = 站点根」契约（openai-images 内补拼 `/v1`，fetchPricing 归一尾缀 `/v1`）；轮询韧性用共享 `pollUntil`（仅对 RelayError status 0/429/5xx 指数退避，终态绝不重试）。工具注册走 M2 实钉的 `DshToolDefinition` 契约（`ctx.tools.register`，模块级 `export const inject = ['tools','webServer']`）。

**Tech Stack:** 沿用 M1/M2（TS strict + NodeNext + strip-types 测试、零运行时依赖、ffmpeg 8.1 本机就绪）。规格：docs/superpowers/specs/2026-09-06-dsh-video-generator-design.md §5/§7.1 + 附录 B。

**范围说明:** 本计划为 M3a。流水线状态机 + vgen_generate + final-cut + 三镜端到端为 M3b（M3a 落地后另出计划）。

---

## 文件结构总览

```
src/
├── providers/openai-images.ts   # 修改：URL 补拼 /v1（拓扑规范化）
├── pricing.ts                   # 修改：归一尾缀 /v1
├── registry.ts                  # 修改：estimate 注入链
├── poll.ts                      # 新增：pollUntil 韧性轮询
├── stages.ts                    # 新增：七段常量表（STAGES，词汇表单一事实源）
├── prompts.ts                   # 新增：四层合并 + 角色三视图模板 + 负面清单 + buildShotPrompt
├── schema/
│   └── handoff.ts               # 新增：story/script/storyboard 类型 + 校验器
└── tools/
    └── handoff.ts               # 新增：vgen_story/vgen_script/vgen_storyboard 工具定义
scripts/
├── probe-relay.ts               # 修改：/v1/models
├── demo-single-shot.ts          # 修改：单通道（站点根）
└── pin-kling-contract.ts        # 修改：站点根语义
test/（各模块配套 .test.ts + tools-handoff.test.ts + 既有测试的拓扑适配）
```

---

### Task 0: 拓扑规范化 + estimate 注入链 + 轮询韧性（M2 三项 MUST-FIX 清偿）

**Files:** Modify `src/providers/openai-images.ts`、`src/pricing.ts`、`src/registry.ts`、`test/openai-images.test.ts`、`test/pricing.test.ts`（新增归一用例）、`test/registry.test.ts`、`scripts/probe-relay.ts`、`scripts/demo-single-shot.ts`、`scripts/pin-kling-contract.ts`；Create `src/poll.ts`、`test/poll.test.ts`

- [ ] **Step 1: 语义测试先行（改既有 + 新增）**

1. `test/openai-images.test.ts`：CHANNEL 的 baseUrl 改为 `https://x.example`（站点根），第一个用例追加断言 `jobId` 为合法 URL 即可（URL 结构断言在 fetch mock 内做：捕获 init，断言被请求的 url 以 `/v1/images/generations` 结尾）——mock 改为捕获 url 并在用例末尾 `assert.ok(seenUrl.endsWith('/v1/images/generations'))`
2. `test/pricing.test.ts` 追加：
```ts
test('fetchPricing 归一尾缀 /v1（用户习惯性粘贴 /v1 不静默 404）', async () => {
  let seenUrl = ''
  const spy = (async (url: unknown) => {
    seenUrl = String(url)
    return new Response(JSON.stringify(SAMPLE), { status: 200 })
  }) as unknown as typeof fetch
  await fetchPricing({ baseUrl: 'https://x.example/v1', apiKey: 'sk-test-123456' }, spy)
  assert.equal(seenUrl, 'https://x.example/api/pricing')
})
```
3. `test/registry.test.ts`：CHANNEL baseUrl 改 `https://x.example`（站点根）
4. 新增 `test/poll.test.ts`：
```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { pollUntil } from '../src/poll.ts'
import { RelayError } from '../src/providers/relay-http.ts'

test('pollUntil：终态立即返回；临时错误指数退避重试；终态绝不重试', async () => {
  let calls = 0
  const r = await pollUntil(
    async () => {
      calls++
      if (calls === 1) throw new RelayError(0, 'network')
      if (calls === 2) throw new RelayError(429, '饱和')
      return { state: 'done' as const, progress: 100 }
    },
    { isFinal: (s) => s.state === 'done' || s.state === 'failed', delayMs: 1, maxPollMs: 5000 },
  )
  assert.equal(r.state, 'done')
  assert.ok(calls >= 3)
})

test('pollUntil：failed 终态不重试直接返回', async () => {
  let calls = 0
  const r = await pollUntil(
    async () => {
      calls++
      return { state: 'failed' as const, progress: null, error: '审核未通过' }
    },
    { isFinal: (s) => s.state === 'done' || s.state === 'failed', delayMs: 1, maxPollMs: 5000 },
  )
  assert.equal(calls, 1)
  assert.equal(r.state, 'failed')
})

test('pollUntil：总超时后抛错（引用最后一次错误）', async () => {
  await assert.rejects(
    pollUntil(
      async () => { throw new RelayError(0, 'network') },
      { isFinal: (s) => s.state === 'done', delayMs: 1, maxPollMs: 50 },
    ),
    /轮询超时/,
  )
})
```

- [ ] **Step 2: 确认失败/RED**（pricing 归一与 poll 模块缺失；openai-images 改造后既有断言变化暴露）

- [ ] **Step 3: 实现**

1. `src/providers/openai-images.ts`：submit 的 URL 从 `${base}/images/generations` 改为 `${base}/v1/images/generations`，注释：`// 拓扑契约：通道 baseUrl = 站点根；OpenAI 兼容端点固定挂 /v1（附录 B.3）。其他站点若不同，走 channel 覆盖（backlog）。`
2. `src/pricing.ts` fetchPricing：base 计算后加 `const root = base.replace(/\/v1$/, '')`，URL 用 `${root}/api/pricing`，注释：`// 归一：用户习惯性粘贴 /v1 结尾时不静默 404`
3. `src/poll.ts`：
```ts
/** 韧性轮询：仅对临时性错误（RelayError status 0/429/5xx）指数退避重试；终态绝不重试；总超时引用最后错误。 */

import { RelayError } from './providers/relay-http.ts'

export interface PollOptions<T> {
  isFinal: (state: T) => boolean
  delayMs?: number
  maxPollMs?: number
  maxDelayMs?: number
}

function isTransient(err: unknown): boolean {
  if (!(err instanceof RelayError)) return false
  const s = err.status
  return s === 0 || s === 429 || s >= 500
}

export async function pollUntil<T>(attempt: () => Promise<T>, opts: PollOptions<T>): Promise<T> {
  const delayMs = opts.delayMs ?? 5000
  const maxDelayMs = opts.maxDelayMs ?? 30000
  const maxPollMs = opts.maxPollMs ?? 600000
  const start = Date.now()
  let delay = delayMs
  let lastErr: unknown = null
  for (;;) {
    try {
      const state = await attempt()
      if (opts.isFinal(state)) return state
      lastErr = null
    } catch (err) {
      if (!isTransient(err)) throw err
      lastErr = err
    }
    if (Date.now() - start > maxPollMs) {
      const detail = lastErr instanceof Error ? lastErr.message : 'unknown'
      throw new Error(`轮询超时（>${Math.round(maxPollMs / 1000)}s）：${detail}`)
    }
    await new Promise((r) => setTimeout(r, delay))
    delay = Math.min(delay * 2, maxDelayMs)
  }
}
```
4. `src/registry.ts`：`providerForModel(channel, model, fetchImpl = fetch)` 扩展为 `providerForModel(channel, model, opts: { fetchImpl?: typeof fetch; estimate?: (model: string) => number | null } = {})`，把 `opts.estimate` 注入三个适配器的 `estimate` 字段（保持旧签名兼容：第二参仍可为 fetchImpl？——**不保持**，M2 的 fetchImpl 参数仅测试用，改为显式 opts 对象并同步更新 test/registry.test.ts 的调用（不传 opts 即可）与内部实现）。
5. `scripts/probe-relay.ts`：URL 改 `${base}/v1/models`（base 语义 = 站点根，用法注释同步）；`scripts/demo-single-shot.ts`：删除 channelRoot 双通道，图像/视频/价目统一用 `VGEN_BASE_URL`（站点根）单通道（openai-images 已补拼 /v1）；`scripts/pin-kling-contract.ts`：用法注释改站点根（路径本就站点根，无代码变化）。
6. 真机回归：`VGEN_BASE_URL=https://api.vectorengine.cn VGEN_API_KEY=$KEY node scripts/probe-relay.ts` → ok:true；`node scripts/demo-single-shot.ts /tmp/vgen-m3a-demo` → 出图出片（花费同 M2 量级）。

- [ ] **Step 4: 全量验证** — `npm test`（84 + 3 = 87 pass 左右）/ typecheck / build / 真机回归
- [ ] **Step 5: Commit** — `git commit -m "fix(m3a): 拓扑规范化（站点根契约）+ estimate 注入链 + pollUntil 轮询韧性"`

---

### Task 1: 七段常量表 + 四层提示词模块

**Files:** Create `src/stages.ts`、`src/prompts.ts`；Test `test/prompts.test.ts`

- [ ] **Step 1: 写失败测试 `test/prompts.test.ts`**

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { STAGES, mergePromptLayers, buildCharacterSheetPrompt, buildScenePrompt, buildShotPrompt, GENERIC_NEGATIVE } from '../src/prompts.ts'

test('STAGES 七段常量：单一事实源（id 有序）', () => {
  assert.deepEqual(STAGES, ['story', 'script', 'storyboard', 'master-asset', 'shot-assets', 'video', 'final-cut'])
})

test('四层合并：dna+模板+手写顺序拼接为正向，injections 独立为负向', () => {
  const r = mergePromptLayers({
    dna: '赛博朋克，霓虹冷调',
    shotTemplate: '中景，缓慢推进',
    manual: '鲸鱼跃出海面',
    injections: ['模糊', '文字水印'],
  })
  assert.equal(r.positive, '赛博朋克，霓虹冷调，中景，缓慢推进，鲸鱼跃出海面')
  assert.equal(r.negative, '模糊，文字水印')
})

test('空层跳过；负向缺省给 GENERIC_NEGATIVE', () => {
  const r = mergePromptLayers({ manual: '一只鲸鱼' })
  assert.equal(r.positive, '一只鲸鱼')
  assert.ok(r.negative.includes('模糊'))
  assert.ok(GENERIC_NEGATIVE.length >= 5)
})

test('角色三视图模板：外观+风格注入，版式与一致性锁在位', () => {
  const p = buildCharacterSheetPrompt({ name: '林鲸', appearance: '蓝色皮肤的小鲸鱼，圆眼睛', style: '3d render, clean style' })
  assert.ok(p.positive.includes('林鲸'))
  assert.ok(p.positive.includes('蓝色皮肤的小鲸鱼'))
  assert.ok(p.positive.includes('三视图') || p.positive.includes('character sheet'))
  assert.ok(p.positive.includes('clean style'))
  assert.ok(p.negative.length > 0)
})

test('场景主图模板：无人物约束注入', () => {
  const p = buildScenePrompt({ name: '海面', description: '清晨的海面，波光粼粼', style: '3d render' })
  assert.ok(p.positive.includes('海面'))
  assert.ok(p.negative.includes('人物'))
})

test('单镜提示词：分镜行+角色锚定+景别运镜合层', () => {
  const p = buildShotPrompt({
    line: '鲸鱼跃出海面溅起水花',
    characterAnchors: ['林鲸（蓝色皮肤的小鲸鱼）'],
    camera: '中景，缓慢推进',
    style: '3d render, clean style',
    referenceHint: '参考图中的角色形象',
  })
  assert.ok(p.positive.includes('鲸鱼跃出海面'))
  assert.ok(p.positive.includes('林鲸（蓝色皮肤的小鲸鱼）'))
  assert.ok(p.positive.includes('中景，缓慢推进'))
  assert.ok(p.positive.includes('参考图中的角色形象'))
})
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现 `src/stages.ts` 与 `src/prompts.ts`**

`src/stages.ts`：
```ts
/** 七段词汇表单一事实源（修 M2 审查遗留：stage 自由字符串散落）。 */

export const STAGES = ['story', 'script', 'storyboard', 'master-asset', 'shot-assets', 'video', 'final-cut'] as const

export type StageId = (typeof STAGES)[number]

export function isStage(s: string): s is StageId {
  return (STAGES as readonly string[]).includes(s)
}
```

`src/prompts.ts`：
```ts
/** 四层提示词（规格 §5：dna/模板/手写顺序即权重；injections 负向）+ 移植鲸影模板精华。 */

export interface PromptLayers {
  dna?: string
  shotTemplate?: string
  manual?: string
  injections?: string[]
}

export interface MergedPrompt {
  positive: string
  negative: string
}

export const GENERIC_NEGATIVE = [
  '模糊', '低分辨率', '文字水印', 'logo', '多余肢体', '面部畸形', '画面割裂', '杂乱背景',
]

export const CHARACTER_NEGATIVE = [
  ...GENERIC_NEGATIVE,
  '视图融合', '面板间特征漂移', '风景背景污染',
]

const clean = (s: string | undefined): string => (s ?? '').trim()

/** 前三层顺序拼接（顺序即权重），injections 独立为负向（空则 GENERIC_NEGATIVE）。 */
export function mergePromptLayers(layers: PromptLayers): MergedPrompt {
  const positive = [clean(layers.dna), clean(layers.shotTemplate), clean(layers.manual)].filter(Boolean).join('，')
  const negative = (layers.injections?.map(clean).filter(Boolean).length ? layers.injections!.map(clean).filter(Boolean) : GENERIC_NEGATIVE).join('，')
  return { positive, negative }
}

export interface CharacterSheetInput {
  name: string
  appearance: string
  style?: string
}

/** 角色三视图卡（一致性锚：版式 + 度量 + 三重一致性锁，移植鲸影 character-sheet 精华）。 */
export function buildCharacterSheetPrompt(input: CharacterSheetInput): MergedPrompt {
  const dna = clean(input.style)
  const shotTemplate = '角色三视图设定图，左区正脸特写，右区侧面/正面/背面三视图，角色高度为画面高度80%，全身可见，纯色浅灰背景，柔和均匀光照，细节一致'
  const manual = `${clean(input.name)}，${clean(input.appearance)}，同一角色在各视图中完全一致，面部特征一致，服装一致`
  const injections = CHARACTER_NEGATIVE
  return mergePromptLayers({ dna, shotTemplate, manual, injections })
}

export interface SceneInput {
  name: string
  description: string
  style?: string
}

/** 场景主图（无人物入镜约束）。 */
export function buildScenePrompt(input: SceneInput): MergedPrompt {
  const dna = clean(input.style)
  const shotTemplate = '场景设定图，空镜，高细节，统一光照方向，景深自然'
  const manual = `${clean(input.name)}，${clean(input.description)}，画面中无人物`
  const injections = [...GENERIC_NEGATIVE, '人物']
  return mergePromptLayers({ dna, shotTemplate, manual, injections })
}

export interface ShotInput {
  line: string
  characterAnchors: string[]
  camera?: string
  style?: string
  referenceHint?: string
}

/** 单镜画面（分镜行 + 角色锚定 + 景别运镜 + 参考图提示）。 */
export function buildShotPrompt(input: ShotInput): MergedPrompt {
  const dna = clean(input.style)
  const shotTemplate = clean(input.camera)
  const anchors = input.characterAnchors.length ? `角色：${input.characterAnchors.join('、')}` : ''
  const ref = clean(input.referenceHint) ? `画面严格参考参考图中的角色形象与风格` : ''
  const manual = [clean(input.line), anchors, ref].filter(Boolean).join('，')
  return mergePromptLayers({ dna, shotTemplate, manual })
}
```

- [ ] **Step 4:** `npm test`（约 93 pass）/ typecheck
- [ ] **Step 5: Commit** — `git commit -m "feat: 七段常量表 + 四层提示词模块（角色三视图/场景/单镜模板+负面清单）"`

---

### Task 2: 三段 Schema 校验器（schema/handoff.ts）

**Files:** Create `src/schema/handoff.ts`；Test `test/schema-handoff.test.ts`

- [ ] **Step 1: 写失败测试 `test/schema-handoff.test.ts`**

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { validateStory, validateScript, validateStoryboard, HandoffError } from '../src/schema/handoff.ts'

const STORY = {
  title: '鲸鱼奇缘',
  logline: '小鲸鱼林鲸跨越大海寻找传说中的鲸群',
  style: '3d render, clean style',
  characters: [{ id: 'linjing', name: '林鲸', appearance: '蓝色皮肤的小鲸鱼，圆眼睛' }],
  chapters: ['第一幕：独自的海洋', '第二幕：风暴与相遇'],
}

test('validateStory：合法结构原样返回；缺字段报 HandoffError', () => {
  assert.equal(validateStory(STORY).title, '鲸鱼奇缘')
  assert.throws(() => validateStory({ title: 'x' }), HandoffError)
  assert.throws(() => validateStory(null), HandoffError)
})

const SCRIPT = {
  ...STORY,
  scenes: [
    { id: 's1', name: '清晨海面', description: '波光粼粼的海面', characters: ['linjing'] },
  ],
  dialog: [
    { sceneId: 's1', characterId: 'linjing', line: '今天也要游过这片海。' },
  ],
}

test('validateScript：场景/对白引用完整性（characterId/sceneId 必须存在）', () => {
  assert.equal(validateScript(SCRIPT).scenes.length, 1)
  assert.throws(() => validateScript({ ...SCRIPT, dialog: [{ sceneId: 's9', characterId: 'linjing', line: 'x' }] }), /sceneId/)
  assert.throws(() => validateScript({ ...SCRIPT, dialog: [{ sceneId: 's1', characterId: 'ghost', line: 'x' }] }), /characterId/)
})

const STORYBOARD = {
  shots: [
    { index: 1, line: '鲸鱼跃出海面', prompt: '鲸鱼跃出海面溅起水花', characterIds: ['linjing'], sceneId: 's1', camera: '中景，缓慢推进', durationSec: 5, voiceHint: '轻快旁白' },
    { index: 2, line: '潜入深海', prompt: '鲸鱼潜入幽蓝深海', characterIds: [], sceneId: 's1', camera: '远景', durationSec: 4 },
  ],
}

test('validateStoryboard：index 连续/时长 2-10/引用存在', () => {
  const sb = validateStoryboard({ ...STORYBOARD, characters: STORY.characters, scenes: SCRIPT.scenes })
  assert.equal(sb.shots.length, 2)
  assert.throws(() => validateStoryboard({ shots: [{ ...STORYBOARD.shots[0], index: 2 }, STORYBOARD.shots[1]] , characters: STORY.characters, scenes: SCRIPT.scenes }), /index/)
  assert.throws(() => validateStoryboard({ shots: [{ ...STORYBOARD.shots[0], durationSec: 30 }], characters: STORY.characters, scenes: SCRIPT.scenes }), /durationSec/)
  assert.throws(() => validateStoryboard({ shots: [{ ...STORYBOARD.shots[0], characterIds: ['ghost'] }], characters: STORY.characters, scenes: SCRIPT.scenes }), /characterIds/)
})
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现 `src/schema/handoff.ts`**（手写校验器，零依赖；类型 + 每段一个 validate 函数；所有错误抛 `HandoffError{code:'bad-request', message}`，message 含字段路径；校验规则如测试：story 必填 title/logline/characters[].{id,name,appearance}/chapters[]；script 追加 scenes[].{id,name,description,characters[]} 与 dialog[].{sceneId,characterId,line} 且引用完整；storyboard：shots[].{index 连续从 1, line, prompt, characterIds[], sceneId?, camera?, durationSec 2-10, voiceHint?} 且 characterIds/sceneId 引用存在于随附 characters/scenes）
- [ ] **Step 4:** `npm test`（约 96 pass）
- [ ] **Step 5: Commit** — `git commit -m "feat: 三段交接 Schema 校验器——引用完整性/时长边界/index 连续"`

---

### Task 3: 交接工具注册（vgen_story/script/storyboard）

**Files:** Create `src/tools/handoff.ts`；Modify `src/host/index.ts`（注册工具 + systemPrompt 通告）；Test `test/tools-handoff.test.ts`

- [ ] **Step 1: 写失败测试 `test/tools-handoff.test.ts`**（工具 execute 层直测，不经 DSH 宿主）

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { buildHandoffTools } from '../src/tools/handoff.ts'
import { VaultStore } from '../src/store/vault.ts'
import { RunStore } from '../src/store/runs.ts'

function ctx() {
  const dir = mkdtempSync(join(tmpdir(), 'vgen-tools-'))
  const vault = VaultStore.open({ file: join(dir, 'vault.json') })
  const runs = RunStore.open({ rootDir: join(dir, 'runs') })
  const tools = buildHandoffTools({ vault, runs })
  return { dir, vault, runs, tools }
}

const STORY = { /* 同 schema 测试的合法 STORY */ }

test('vgen_story：开 run 并落盘 story.json；缺字段走 HandoffError 信封', async () => {
  const c = ctx()
  try {
    const story = c.tools.story
    const r = await story.execute({ story: STORY }) as { ok: boolean; value: { runId: string; stages: string[] } }
    assert.equal(r.ok, true)
    assert.ok(r.value.runId.startsWith('run-'))
    assert.deepEqual(r.value.stages, ['story'])
    const bad = await story.execute({ story: { title: 'x' } }) as { ok: boolean; error: { code: string } }
    assert.equal(bad.ok, false)
    assert.equal(bad.error.code, 'bad-request')
    const got = c.runs.get(r.value.runId)
    assert.equal(got?.stages['story'], 'done')
  } finally {
    rmSync(c.dir, { recursive: true, force: true })
  }
})

test('vgen_script：挂到已有 run；未知 runId 报 not-found', async () => {
  const c = ctx()
  try {
    const open = await c.tools.story.execute({ story: STORY }) as { value: { runId: string } }
    const r = await c.tools.script.execute({ runId: open.value.runId, script: { ...SCRIPT } }) as { ok: boolean }
    assert.equal(r.ok, true)
    const miss = await c.tools.script.execute({ runId: 'run-nope', script: { ...SCRIPT } }) as { ok: boolean; error: { code: string } }
    assert.equal(miss.error.code, 'not-found')
  } finally {
    rmSync(c.dir, { recursive: true, force: true })
  }
})

test('vgen_storyboard：分镜校验 + 四层提示词注入落盘 storyboard.json', async () => {
  const c = ctx()
  try {
    const open = await c.tools.story.execute({ story: STORY }) as { value: { runId: string } }
    await c.tools.script.execute({ runId: open.value.runId, script: { ...SCRIPT } })
    const r = await c.tools.storyboard.execute({
      runId: open.value.runId,
      shots: [
        { index: 1, line: '鲸鱼跃出海面', prompt: '', characterIds: ['linjing'], sceneId: 's1', camera: '中景，缓慢推进', durationSec: 5 },
      ],
      style: '3d render, clean style',
    }) as { ok: boolean; value: { shots: Array<{ prompt: string }> } }
    assert.equal(r.ok, true)
    assert.ok(r.value.shots[0]!.prompt.includes('鲸鱼跃出海面'))
    assert.ok(r.value.shots[0]!.prompt.includes('3d render'))
  } finally {
    rmSync(c.dir, { recursive: true, force: true })
  }
})
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现 `src/tools/handoff.ts`**

```ts
/** LLM 三段交接工具（规格 §5 表）：会话模型产出结构化 JSON → 校验 + 落盘 + run 推进。 */

import { mkdirSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import type { VaultStore } from '../store/vault.ts'
import type { RunStore } from '../store/runs.ts'
import { validateStory, validateScript, validateStoryboard, HandoffError, type Story, type Script, type Storyboard } from '../schema/handoff.ts'
import { buildShotPrompt } from '../prompts.ts'
import { STAGES } from '../stages.ts'

export interface HandoffContext {
  vault: VaultStore
  runs: RunStore
}

export type ToolResult = { ok: true; value: unknown } | { ok: false; error: { code: string; message: string } }

function wrap<T>(fn: () => T): ToolResult {
  try {
    return { ok: true, value: fn() }
  } catch (err) {
    if (err instanceof HandoffError) return { ok: false, error: { code: err.code, message: err.message } }
    return { ok: false, error: { code: 'internal', message: err instanceof Error ? err.message : String(err) } }
  }
}

function runDir(runs: RunStore, runId: string): string {
  return join(runs.rootDir, runId)
}

function persist(runs: RunStore, runId: string, name: 'story' | 'script' | 'storyboard', data: unknown): void {
  mkdirSync(runDir(runs, runId), { recursive: true })
  writeFileSync(join(runDir(runs, runId), `${name}.json`), JSON.stringify(data, null, 2))
}

function requireRun(runs: RunStore, runId: unknown): string {
  const id = typeof runId === 'string' ? runId : ''
  if (!runs.get(id)) throw new HandoffError('not-found', `run 不存在: ${id}`)
  return id
}

export interface HandoffTools {
  story: { execute: (args: { story: unknown }) => Promise<ToolResult> }
  script: { execute: (args: { runId: string; script: unknown }) => Promise<ToolResult> }
  storyboard: { execute: (args: { runId: string; shots: unknown; style?: string }) => Promise<ToolResult> }
}

export function buildHandoffTools(ctx: HandoffContext): HandoffTools {
  const { vault, runs } = ctx
  void vault
  return {
    story: {
      execute: async (args) => wrap(() => {
        const story = validateStory(args['story'])
        const run = runs.create(story.title)
        persist(runs, run.id, 'story', story)
        runs.setStage(run.id, 'story', 'done')
        runs.appendEvent(run.id, 'stage-done', { stage: 'story' })
        return { runId: run.id, stages: STAGES.slice(0, 1), next: '调用 vgen_script 提交剧本' }
      }),
    },
    script: {
      execute: async (args) => wrap(() => {
        const runId = requireRun(runs, args['runId'])
        const script = validateScript(args['script'])
        persist(runs, runId, 'script', script)
        runs.setStage(runId, 'script', 'done')
        runs.appendEvent(runId, 'stage-done', { stage: 'script' })
        return { runId, stages: STAGES.slice(0, 2), next: '调用 vgen_storyboard 提交分镜' }
      }),
    },
    storyboard: {
      execute: async (args) => wrap(() => {
        const runId = requireRun(runs, args['runId'])
        const raw = args['shots'] as { shots?: unknown; characters?: unknown; scenes?: unknown }
        // 分镜的 characters/scenes 从已落盘 script 取，调用方只传 shots + style
        const script = JSON.parse(readFileSync(join(runDir(runs, runId), 'script.json'), 'utf8')) as Script
        const storyboard = validateStoryboard({
          shots: raw?.shots,
          characters: script.characters,
          scenes: script.scenes,
        })
        const style = typeof args['style'] === 'string' ? args['style'] : (script as { style?: string }).style ?? ''
        const enriched = storyboard.shots.map((shot) => {
          const anchors = shot.characterIds.map((cid) => {
            const ch = script.characters.find((c) => c.id === cid)
            return ch ? `${ch.name}（${ch.appearance}）` : cid
          })
          const merged = buildShotPrompt({ line: shot.line, characterAnchors: anchors, camera: shot.camera, style, referenceHint: '参考图' })
          return { ...shot, positive: merged.positive, negative: merged.negative }
        })
        persist(runs, runId, 'storyboard', { shots: enriched })
        runs.setStage(runId, 'storyboard', 'done')
        runs.appendEvent(runId, 'stage-done', { stage: 'storyboard' })
        return { runId, stages: STAGES.slice(0, 3), shots: enriched.map((s) => ({ index: s.index, prompt: s.positive })) }
      }),
    },
  }
}
```
注意：实现需补 `readFileSync` import；`validateStoryboard` 的入参形状（shots 与 characters/scenes 分离）与 Task 2 测试的传参方式（包在一个对象）保持一致——以 Task 2 实现的函数签名为准调整此处调用，两任务实现者需读对方产物（本任务实施者先读 src/schema/handoff.ts）。
`HandoffError` 需在 schema/handoff.ts 导出（Task 2 实现时确认 code: 'bad-request' | 'not-found'，not-found 由工具层的 requireRun 抛）。

- [ ] **Step 4:** `npm test`（约 99 pass）
- [ ] **Step 5: Modify `src/host/index.ts`**——注册三工具 + Agent 通告：
  - `export const inject = ['webServer', 'tools']`
  - `ctx.inject(['tools'], (svc) => { const tools = svc as { register: (def: unknown) => () => void }; const handoff = buildHandoffTools({ vault, runs }); for (const def of handoffToolDefs(handoff)) ctx.effect(() => tools.register(def), \`dsh-video-generator: \${def.name}\`) })`
  - 新增 `src/tools/defs.ts` 或在 handoff.ts 导出 `handoffToolDefs(handoff): DshToolDefinition[]`（DshToolDefinition 形状照 super-ppts：name/description/parameters(JSON Schema)/output.{schema,render}/timeoutMs/execute；render 统一 `(_args, value) => [{type:'text', text: JSON.stringify(value)}]`；三个工具的 parameters 分别为 {story: object} / {runId: string, script: object} / {runId: string, shots: array, style?: string}）
  - `export const SYSTEM_PROMPT_SECTION`（对齐 super-ppts 的 systemPrompt.section 用法，加入 SUPER_PPTS_GUIDANCE 同款通告文本：何时用三工具、JSON 形状示例、之后接 vgen_generate）
- [ ] **Step 6: 全量验证** — npm test / typecheck / build；真机 boot（M1 Task 10 的 dsh CLI 流程）curl 无法直接调工具——改为在 DSH 会话里冒烟：装载后让会话模型调 vgen_story（人工步骤，输出留档）
- [ ] **Step 7: Commit** — `git commit -m "feat: vgen 三段交接工具——schema 校验/落盘/run 推进/工具注册+通告"`

---

### Task 4: M3a 收尾验证

- [ ] 全量：`npm test` / `npm run typecheck` / `npm run build` 全绿
- [ ] 真机 boot：隔离 profile 装载 → 会话冒烟（vgen_story → vgen_script → vgen_storyboard 三连，产物落 runs/<runId>/）→ remove 清理
- [ ] Commit 遗留 + M3b 计划输入确认（RunStore 段状态/prompt 已就位）

---

## 自审记录

1. **规格覆盖（M3a 范围）**：MUST-FIX 三项（Task 0）、§5 表三工具（Task 3）、四层提示词（Task 1）、§5.1 run 落盘（Task 3 persist）、§7.1 工具契约（DshToolDefinition 实钉）。流水线/final-cut 明确留给 M3b。
2. **占位扫描**：Task 3 的 `STORY`/`SCRIPT` 引用测试文件内已有常量（注释指明"同 schema 测试的合法 STORY"，实施者复制即可，非外部引用占位）；schema/handoff 与 tools/handoff 的签名衔接已显式指示"以 Task 2 实现为准调整"。
3. **类型一致性**：`HandoffError` 在 Task 2 导出、Task 3 消费；`buildShotPrompt`（Task 1）被 Task 3 消费；`STAGES`（Task 1）被 Task 3 消费；`confirmSpend`/registry opts 变更（Task 0）已同步说明调用方更新点。
